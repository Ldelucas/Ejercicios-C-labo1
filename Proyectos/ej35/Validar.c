#include <stdio.h>
#include <stdlib.h>
#include "Validar.h"

int Validar (int num)
  {
       
            
            if(num> -100 && num< 100)
               {
                    return 0;
               }else{
                    return 1; 
               }
             
                
  }
